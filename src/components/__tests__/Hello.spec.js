import { describe, it, expect } from 'vitest'

//import { mount } from '@vue/test-utils'

describe('Hello', () => {
  it('shame that there is no unit tests', () => {
    expect(true).toBe(true)
  })
})
